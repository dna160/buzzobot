{
  "$comment": "Generated by scripts/export_content_schema.py \u2014 do not edit by hand. Regenerate after changing engine/contracts/content.py.",
  "$defs": {
    "Actionability": {
      "enum": [
        "high",
        "medium",
        "low"
      ],
      "title": "Actionability",
      "type": "string"
    },
    "CardCopy": {
      "description": "Per-finding prose for a deck card (Brief Deck PRD \u00a73.2's `FindingCard`).\n\nThe agents narrate *sections*, not findings \u2014 one draft per section \u2014 so a\ndeck that wants one card per finding has nowhere to get the words. This is\nthat source: the same closed `claim_frame` template table the instant tier\nwrites from, rendered for every finding, in both tiers.\n\nDeliberately deterministic even on the full tier. A card is evidence-first\n(its numbers are chips straight out of `evidence`), and the narrated\nanalysis stays where the agents actually wrote it \u2014 the section prose above\nthe cards. Mixing agent prose into some cards and templates into others\nwould make \"who wrote this sentence\" unanswerable from the deck.",
      "properties": {
        "action": {
          "title": "Action",
          "type": "string"
        },
        "headline": {
          "title": "Headline",
          "type": "string"
        },
        "mechanism": {
          "title": "Mechanism",
          "type": "string"
        }
      },
      "required": [
        "headline",
        "mechanism",
        "action"
      ],
      "title": "CardCopy",
      "type": "object"
    },
    "Comparison": {
      "properties": {
        "baseline_value": {
          "title": "Baseline Value",
          "type": "number"
        },
        "basis": {
          "$ref": "#/$defs/ComparisonBasis"
        },
        "current_value": {
          "title": "Current Value",
          "type": "number"
        },
        "delta_abs": {
          "title": "Delta Abs",
          "type": "number"
        },
        "delta_pct": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Delta Pct"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Label"
        }
      },
      "required": [
        "basis",
        "baseline_value",
        "current_value",
        "delta_abs"
      ],
      "title": "Comparison",
      "type": "object"
    },
    "ComparisonBasis": {
      "enum": [
        "prior_period",
        "cohort_median",
        "account_baseline"
      ],
      "title": "ComparisonBasis",
      "type": "string"
    },
    "Confidence": {
      "enum": [
        "high",
        "medium",
        "low"
      ],
      "title": "Confidence",
      "type": "string"
    },
    "CoverageAudit": {
      "description": "G08's audit, exported as data instead of only as prose. Every field here\nis read from the G08 finding's own `evidence` dict or from the generator\nbattery's coverage gaps \u2014 nothing is re-derived, so the numeral gate's\nguarantee extends to the coverage lines a deck prints.",
      "properties": {
        "active_day_coverage_pct": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Active Day Coverage Pct"
        },
        "assessed_confidence": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Assessed Confidence"
        },
        "current_active_days": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Current Active Days"
        },
        "generator_gaps": {
          "items": {
            "$ref": "#/$defs/GeneratorGap"
          },
          "title": "Generator Gaps",
          "type": "array"
        },
        "missing_preferred_metrics": {
          "items": {
            "type": "string"
          },
          "title": "Missing Preferred Metrics",
          "type": "array"
        },
        "missing_required_metrics": {
          "items": {
            "type": "string"
          },
          "title": "Missing Required Metrics",
          "type": "array"
        },
        "recency_lag_days": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Recency Lag Days"
        },
        "signals": {
          "additionalProperties": {
            "$ref": "#/$defs/SectionSignals"
          },
          "title": "Signals",
          "type": "object"
        }
      },
      "title": "CoverageAudit",
      "type": "object"
    },
    "Direction": {
      "enum": [
        "positive",
        "negative",
        "neutral"
      ],
      "title": "Direction",
      "type": "string"
    },
    "EntityRef": {
      "description": "Which specific thing a `Finding` is about. `FindingLevel` (a sibling\nfield on `Finding`, not here) states what *kind* of thing \u2014 kept\nseparate per PRD \u00a73.1's explicit-level rule, not folded into this ref.",
      "properties": {
        "display_name": {
          "title": "Display Name",
          "type": "string"
        },
        "id": {
          "title": "Id",
          "type": "string"
        }
      },
      "required": [
        "id",
        "display_name"
      ],
      "title": "EntityRef",
      "type": "object"
    },
    "Finding": {
      "description": "One computed, ranked, narratable observation. Per PRD \u00a76.1, plus\n`tenant_id` \u2014 not in the PRD's abbreviated snippet, but Hard Rule 6\n(\"tenant_id bound at the API boundary, carried in every typed payload,\nasserted at every graph node\") is unambiguous and the snippet is\nillustrative rather than exhaustive (it also omits pydantic's own\n`model_config`, which nobody would argue should be left out).",
      "properties": {
        "actionability": {
          "$ref": "#/$defs/Actionability"
        },
        "brief_type": {
          "enum": [
            "awareness",
            "gmv",
            "install"
          ],
          "title": "Brief Type",
          "type": "string"
        },
        "caveats": {
          "items": {
            "type": "string"
          },
          "title": "Caveats",
          "type": "array"
        },
        "claim_frame": {
          "title": "Claim Frame",
          "type": "string"
        },
        "comparison": {
          "anyOf": [
            {
              "$ref": "#/$defs/Comparison"
            },
            {
              "type": "null"
            }
          ]
        },
        "confidence": {
          "$ref": "#/$defs/Confidence"
        },
        "direction": {
          "$ref": "#/$defs/Direction"
        },
        "entity": {
          "$ref": "#/$defs/EntityRef"
        },
        "evidence": {
          "additionalProperties": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "integer"
              },
              {
                "type": "string"
              }
            ]
          },
          "title": "Evidence",
          "type": "object"
        },
        "generator": {
          "title": "Generator",
          "type": "string"
        },
        "id": {
          "title": "Id",
          "type": "string"
        },
        "level": {
          "$ref": "#/$defs/FindingLevel"
        },
        "magnitude_pct": {
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Magnitude Pct",
          "type": "number"
        },
        "materiality": {
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Materiality",
          "type": "number"
        },
        "origin": {
          "$ref": "#/$defs/Origin",
          "default": "generator"
        },
        "provenance": {
          "items": {
            "type": "string"
          },
          "title": "Provenance",
          "type": "array"
        },
        "section_affinity": {
          "items": {
            "type": "integer"
          },
          "minItems": 1,
          "title": "Section Affinity",
          "type": "array"
        },
        "tenant_id": {
          "title": "Tenant Id",
          "type": "string"
        }
      },
      "required": [
        "id",
        "generator",
        "tenant_id",
        "brief_type",
        "section_affinity",
        "entity",
        "level",
        "claim_frame",
        "evidence",
        "comparison",
        "magnitude_pct",
        "direction",
        "actionability",
        "confidence",
        "materiality"
      ],
      "title": "Finding",
      "type": "object"
    },
    "FindingLevel": {
      "description": "The entity granularity a `Finding` is about (PRD \u00a76.1). Hard Rule 2:\n\"Never sum across levels\" \u2014 every `Finding` states unambiguously which\nsingle level it is, so a property test can grep for a finding whose\n`evidence` mixes figures from two levels without inspecting prose.\n\nPRD \u00a76.1's literal type omits `account`, but G01 (identity\ndecomposition) and G07 (comparability normalizer) \u2014 arguably the two\nhighest-value generators in the catalogue (\u00a76.3 calls G01 \"highest-value\ngenerator\" outright) \u2014 both operate on the whole-account rollup, not any\nsingle campaign/adgroup/etc. Excluding `account` would make those two\ngenerators unable to produce a schema-valid `Finding` at all. Added here\nas a corrected reading rather than worked around; flagged for review.",
      "enum": [
        "account",
        "campaign",
        "adgroup",
        "creative",
        "product",
        "creator",
        "session"
      ],
      "title": "FindingLevel",
      "type": "string"
    },
    "GeneratorGap": {
      "description": "One generator that could not run, and why (`CoverageGap` as exported).\nPRD \u00a76.2's \"skip silently and register a coverage gap\" \u2014 the deck renders\nthese as counted denominators, so silence never reads as coverage.",
      "properties": {
        "generator": {
          "title": "Generator",
          "type": "string"
        },
        "missing_metrics": {
          "items": {
            "type": "string"
          },
          "title": "Missing Metrics",
          "type": "array"
        },
        "reason": {
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "generator",
        "reason"
      ],
      "title": "GeneratorGap",
      "type": "object"
    },
    "Origin": {
      "description": "Whether a `Finding` came from the fixed generator battery or the\nanalyst agent's probe loop (PRD \u00a75.2, B7 \u2014 not built until B7, but the\nfield exists from B1 so nothing downstream needs a schema migration\nwhen the probe loop lands).",
      "enum": [
        "generator",
        "probe"
      ],
      "title": "Origin",
      "type": "string"
    },
    "ProbeLoopExport": {
      "properties": {
        "enabled": {
          "default": false,
          "title": "Enabled",
          "type": "boolean"
        },
        "log": {
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "title": "Log",
          "type": "array"
        },
        "probes_executed": {
          "default": 0,
          "title": "Probes Executed",
          "type": "integer"
        },
        "rounds_used": {
          "default": 0,
          "title": "Rounds Used",
          "type": "integer"
        },
        "yield_rate": {
          "default": 0.0,
          "title": "Yield Rate",
          "type": "number"
        }
      },
      "title": "ProbeLoopExport",
      "type": "object"
    },
    "RankedEntry": {
      "properties": {
        "id": {
          "title": "Id",
          "type": "string"
        },
        "materiality": {
          "title": "Materiality",
          "type": "number"
        }
      },
      "required": [
        "id",
        "materiality"
      ],
      "title": "RankedEntry",
      "type": "object"
    },
    "SectionEntry": {
      "description": "One narrated section. `draft` is a `SectionDraftFull` or\n`SectionDraftLow` (Hard Rule 8: the low tier has no `implication` field at\nall), kept as a dict here rather than a union so the confidence ladder stays\nowned by `llm/schemas.py` and this boundary does not fork it.",
      "properties": {
        "critic_approved": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Critic Approved"
        },
        "critic_notes": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Critic Notes"
        },
        "critic_ok": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Critic Ok"
        },
        "draft": {
          "additionalProperties": true,
          "title": "Draft",
          "type": "object"
        },
        "narration_attempts": {
          "default": 0,
          "title": "Narration Attempts",
          "type": "integer"
        },
        "narration_source": {
          "title": "Narration Source",
          "type": "string"
        }
      },
      "required": [
        "draft",
        "narration_source"
      ],
      "title": "SectionEntry",
      "type": "object"
    },
    "SectionRankingExport": {
      "description": "Per section: what was narrated, and what lost. PRD \u00a76.5 \u2014 \"persist the\nfull ranked list including everything below the cut, that is the only\nsignal that later tells us the presets are wrong.\" Lampiran C renders it.",
      "properties": {
        "below_cut": {
          "items": {
            "$ref": "#/$defs/RankedEntry"
          },
          "title": "Below Cut",
          "type": "array"
        },
        "selected": {
          "items": {
            "type": "string"
          },
          "title": "Selected",
          "type": "array"
        }
      },
      "title": "SectionRankingExport",
      "type": "object"
    },
    "SectionSignals": {
      "description": "\"4 dari 6 sinyal tersedia\" for one section.\n\n`available` counts the distinct generators that actually contributed a\nfinding to this section. `total` adds every generator that skipped with a\ncoverage gap \u2014 deliberately conservative: a generator that never ran has no\nfindings, so its section affinity is unknowable, and counting it against\nevery section can understate coverage but can never overstate it.",
      "properties": {
        "available": {
          "minimum": 0,
          "title": "Available",
          "type": "integer"
        },
        "total": {
          "minimum": 0,
          "title": "Total",
          "type": "integer"
        }
      },
      "required": [
        "available",
        "total"
      ],
      "title": "SectionSignals",
      "type": "object"
    },
    "SynthesisEntry": {
      "description": "S1 or S6, plus how it was produced. `source` distinguishes the agent path\nfrom the deterministic writer \u2014 the deck's provenance footer prints it, per\nthe doctrine that a thin brief must say it is thin.",
      "properties": {
        "attempts": {
          "default": 0,
          "title": "Attempts",
          "type": "integer"
        },
        "draft": {
          "additionalProperties": true,
          "title": "Draft",
          "type": "object"
        },
        "fallback_reason": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Fallback Reason"
        },
        "source": {
          "title": "Source",
          "type": "string"
        }
      },
      "required": [
        "draft",
        "source"
      ],
      "title": "SynthesisEntry",
      "type": "object"
    }
  },
  "description": "What `GET /v1/briefs/{run_id}` and `POST /v1/briefs/sync` put under\n`content`. `content_version` is the discriminator a caller checks before\nreading anything below `s1`/`sections`/`s6`.",
  "properties": {
    "brief_type": {
      "title": "Brief Type",
      "type": "string"
    },
    "card_copy": {
      "additionalProperties": {
        "$ref": "#/$defs/CardCopy"
      },
      "title": "Card Copy",
      "type": "object"
    },
    "content_version": {
      "const": 2,
      "default": 2,
      "title": "Content Version",
      "type": "integer"
    },
    "coverage": {
      "anyOf": [
        {
          "$ref": "#/$defs/CoverageAudit"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "engine_version": {
      "title": "Engine Version",
      "type": "string"
    },
    "findings": {
      "items": {
        "$ref": "#/$defs/Finding"
      },
      "title": "Findings",
      "type": "array"
    },
    "probe_loop": {
      "$ref": "#/$defs/ProbeLoopExport"
    },
    "rankings": {
      "additionalProperties": {
        "$ref": "#/$defs/SectionRankingExport"
      },
      "title": "Rankings",
      "type": "object"
    },
    "s1": {
      "anyOf": [
        {
          "$ref": "#/$defs/SynthesisEntry"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "s6": {
      "anyOf": [
        {
          "$ref": "#/$defs/SynthesisEntry"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "sections": {
      "additionalProperties": {
        "$ref": "#/$defs/SectionEntry"
      },
      "title": "Sections",
      "type": "object"
    },
    "tier": {
      "enum": [
        "instant",
        "full"
      ],
      "title": "Tier",
      "type": "string"
    }
  },
  "required": [
    "tier",
    "engine_version",
    "brief_type"
  ],
  "title": "BriefContentV2",
  "type": "object"
}
